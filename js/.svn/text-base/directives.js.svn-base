'use strict';

/* Directives */

angular.module('angularjs-starter', ['$strap.directives']);
angular.module('nexus.directives', []).
  directive('appVersion', ['version', function(version) {
    return function(scope, elm, attrs) {
      elm.text(version);
    };
  }]);
  
 